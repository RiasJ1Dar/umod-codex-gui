# UMOD Codex GUI — редизайн вікна

Макети: [канва редизайну](https://claude.ai/artifact/NsaMJoNH1ZuzeVKgBBp41b)
(приватна — відкривається лише власнику і тим, кому він дав доступ).
Чотири артборди: «Очікування», «Проксі працює», «Проблема», «Токени».

Джерело правди в коді — `src/theme.rs`. Якщо макет і `theme.rs` розійдуться,
правий `theme.rs`: канва описує намір, код описує те, що збирається.

## Що було не так

| Було | Стало |
|---|---|
| Вміст впритул до країв вікна — `eframe` віддає `Ui` без полів і тла | Тло і смуга шапки малюються явно, вміст має поле 20 px |
| Статус проксі — крапка в рядку серед інших рядків | Картка над формою: колір тла, рамки і крапки; адреса, аптайм |
| Три кнопки однакової ваги | Головна дія на всю ширину, 46 px, акцентна заливка; пуск/стоп — 38 px, другорядні |
| Кнопки активні завжди, натиск нічого не робить | Неактивні, коли дія неможлива: «Стоп» без проксі, «Запустити» без credential |
| Попередження про кілька `.env` губилось у журналі | Стоїть під полем, поле і кнопка беруть бурштинову рамку |
| Журнал — плаский список під `separator` | Врізана панель, моноширинний час окремою колонкою, кнопка «скопіювати» |
| Статус при старті завжди «не працює», навіть якщо проксі піднятий | Разова проба порту у фоні, час проби в картці |

## Палітра

Темна, один акцент. Зелений означає «справно» і водночас «головна дія» —
тому кнопка запуску і стан «працює» одного кольору. Бурштин і червоний —
лише сигнали, у навігації не беруть участі.

| Роль | Колір |
|---|---|
| тло вікна | `#15171C` |
| шапка | `#1A1D23` |
| картка, друга кнопка | `#1E2127` |
| поле вводу | `#14171B` |
| журнал | `#101215` |
| лінія 1 px | `#2C3037` |
| основний текст | `#E4E6EA` |
| другорядний | `#C6CBD3` |
| підписи, час | `#8C94A0` |
| акцент, «працює» | `#62A97B` |
| попередження | `#D8A648` |
| помилка | `#DE6F65` |

Контраст тексту до свого тла — не нижче 4.5:1, зокрема `#8C94A0` на `#101215`.

## Метрика

- вікно 520 × 680, мінімум 440 × 560
- поле по периметру 20, крок між блоками 16
- контрол 38 px, головна кнопка 46 px
- заокруглення: 10 картка, 8 поле і кнопка
- шрифти — ті, що вже в `default_fonts` egui: пропорційний для інтерфейсу,
  моноширинний для часу, адрес і шляхів

## Два обмеження egui, які вплинули на код

**Немає letter-spacing.** Заголовок і підписи секцій викладаються поштучно —
`theme::tracked_text`.

**Покриття гліфів не гарантоване.** `▶`, `■`, `⟳` замінені на фігури, які
малює painter (`src/ui.rs`): на будь-якій системі виходить піктограма, а не
порожня рамка.

## Файли

```
src/theme.rs   токени, стиль egui, помічники малювання
src/ui.rs      піктограми, кнопка з піктограмою, кнопка-піктограма
src/main.rs    збірка вікна з цих частин + логіка, що була
```
