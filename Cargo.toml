[package]
name = "umod-codex-gui"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"

[dependencies]
eframe = { version = "0.36", default-features = false, features = ["default_fonts", "glow"] }
egui = "0.36"
log = "0.4"

[profile.release]
opt-level = 2
strip = true
