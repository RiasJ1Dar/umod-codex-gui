# UMOD Codex GUI — стан на 2026-09-18

## Що це

Rust GUI-лаунчер для запуску Codex CLI з профілем `umod`.
Вікно: вибрав модель → «Запустити» → відкрився Codex.
Тека: `C:\Users\Гартунг\Desktop\umod-codex-gui`
GitHub: https://github.com/RiasJ1Dar/umod-codex-gui (публічний)

## Що готово

### Код — написаний, компілюється, зібрано ✅

- `cargo check --offline` — 0 помилок, 0 warnings
- `cargo build --release --offline` — Finished, 6.7 МБ .exe
- `target/release/umod-codex-gui.exe`

### Файли

- `src/main.rs` — GUI на egui 0.36: вибір моделі, credential, статус проксі,
  кнопки «Запустити Codex» / «Пуск» / «Стоп», журнал
- `src/proxy.rs` — логіка проксі: пошук credential, HTTP-перевірка /v1/models,
  старт/стоп, Arc<Mutex<ProxyState>>
- `src/codex.rs` — запуск Codex CLI (без --port, бо Codex не має такого прапорця)
- `Cargo.toml` — eframe 0.36, default-features=false, features=[default_fonts, glow]
- `.cargo/config.toml` — vendor source
- `vendor/` — ~1050 крейтів (не в git, качаються скриптом)
- `PLAN.md` — оновлений Claude з детальним аудитом

### Аудит Claude (з worktree) — виправлено

| Етап | Баг | Стан |
|---|---|---|
| 1 | cargo check помилки (checked_sub, типи, імпорти) | ✅ |
| 2 | Вікно зависне (contains з малої літери) | ✅ ProxyMsg enum |
| 3 | --port не існує у Codex | ✅ прибрано |
| 3 | TCP замість HTTP-проби | ✅ GET /v1/models |
| 4 | mem::forget губить handle | ✅ Arc<Mutex> + кнопка Стоп |
| 5 | Тихий None при 2+ .env | ✅ warning в журналі |
| 6 | Консольне вікно блимає | ✅ windows_subsystem=windows |
| 6 | checked_sub для f32 | ✅ stick_to_bottom |
| 6 | UTC замість локального | ✅ +3 Kiev |

## Що НЕ готово

### Не тестовано наживо

- Запуск GUI: чи відкривається вікно
- Кнопка «Пуск проксі»: чи піднімається проксі
- Кнопка «Запустити Codex»: чи відкривається Codex
- Кнопка «Стоп проксі»: чи зупиняється проксі
- Індикатор статусу: чи міняється після Пуск/Стоп

### З PLAN.md (Етапи 6-8)

- [ ] Іконка (.ico) — потрібен embed-resource або rc, ще одна залежність
- [ ] Збереження налаштувань між запусками (модель, credential) — JSON в %APPDATA%
- [ ] `now_str()` — hardcoded +3, треба chrono або системний локальний час
- [ ] Юніт-тести find_credential (0, 1, 2 файли)
- [ ] macOS збірка
- [ ] Cargo.lock закомітити в git (зараз в .gitignore)

### Середовище

- ESET блокує мережу всім Windows-процесам (powershell, pwsh, git, cargo)
- Мережа тільки через WSL2
- Git/push через `bash -c "git push origin main"`
- Commit message через `-F <файл>`, не через `-m`
- Credential Manager: `git config --global credential.helper
  '/mnt/c/Program\ Files/Git/mingw64/bin/git-credential-manager.exe'`

## UMOD

- Credential: `~/.umod/a5081 (1).env` (видано 2026-09-18)
- Проксі: `~/Desktop/umod-codex-client/bin/umod-token-proxy` (Python, порт 8787)
- PowerShell обгортка: `~/Desktop/umod-codex-client/bin/Umod-Codex.ps1`
- Моделі: glm-5.2, gpt-5.6-sol/terra/luna, gpt-6-astra, deepseek-v4.1-flash
- config.toml: `~/Desktop/umod-codex-client/config.toml.example`

## Коміти

| SHA | Що |
|---|---|
| 87f605a | Початковий код |
| 7b4e167 | .gitignore: .env |
| 7ea84b4 | README |
| 403586c | egui 0.36 API fix + vendor |
| 57fbdba | Виправити баги з аудиту PLAN.md |

## Скрипти (у теці проєкту)

- `build_loop3.ps1` — цикл докачування крейтів + збірка
- `vendor.ps1` — розпакування vendor
- `fix_objc.ps1` — фікс objc-sys вкладеності

## Що робити далі

1. **Тест наживо** — запустити .exe, перевірити вікно, кнопки, проксі
2. Закомітити Cargo.lock (прибрати з .gitignore)
3. Іконка
4. Збереження налаштувань
5. Юніт-тести
6. macOS
